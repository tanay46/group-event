<div class="event">
<h2><%= link_to event.name, event %></h2>
<h3>On <%= event.date %></h3>

<% if current_user.role == "Admin" %>
<%= link_to 'Edit', edit_event_path(event), :class => 'btn btn-primary' %>
<%= link_to 'Delete', event, confirm: 'Are you sure?', method: :delete, :class => 'btn btn-primary' %></p>
<% end %>
</div>